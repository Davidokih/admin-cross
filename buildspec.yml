version: 0.2

phase: 
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - rcho Build completed on `successfully`
artfacts:
  files:
    - "**/*"
  discard-paths: no
  base-directory: build 